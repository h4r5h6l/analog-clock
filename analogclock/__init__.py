"""Analog Clock desktop widget package.

Split out of the former monolithic ``clock.py`` so that platform
workarounds, persistence, colors, timezones, hardware polling, drawing,
the settings dialog and the widget itself each live in one focused
module. ``main.py`` at the project root is the entry point.
"""
